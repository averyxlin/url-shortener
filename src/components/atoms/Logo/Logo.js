import React from 'react';
import './Logo.css';

const Logo = () => {
  return (
    <div className="logo">
      <span className="logo-url">URL</span>
      <span className="logo-shortener">SHORTENER</span>
    </div>
  );
};

export default Logo;