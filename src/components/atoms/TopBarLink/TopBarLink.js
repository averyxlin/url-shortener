import React from 'react'

const TopBarLink = ({title, onClick}) => {
  return (
    <div className="top-bar-link">
      <div className="top-bar-link-title">{title}</div>
      
    </div>
  )
}

export default TopBarLink